#include <stdlib.h>
extern double calcola_seno(double x);
extern double pot(double base, double esponente);
extern double fact(double base);

int main(void) {

	double ris = calcola_seno(1.0);

	double prova = pot(9.0, 2.0);
	double a = fact(9.0);


	return 0;
}